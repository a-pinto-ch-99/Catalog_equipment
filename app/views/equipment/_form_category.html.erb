<%= form_for(@category, :url => add_category_path(@category, format: :json), remote: true, :html => { :class => "form-group"}) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cat_name %><br>
    <%= f.text_field :cat_name, :class => "form-control" %>
  </div>
  <div class="field">
    <%= f.label :cat_description %><br>
    <%= f.text_field :cat_description, :class => "form-control" %>
  </div>
  <div></div>
  <div class="actions">
    <%= f.submit 'Create a New Category', :class => "btn btn-primary", :id => "form_otf" %>
  </div>
<% end %>
